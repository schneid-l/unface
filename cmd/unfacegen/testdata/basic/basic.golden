// Code generated by unfacegen; DO NOT EDIT.

package basic

import (
	"time"

	"github.com/schneid-l/unface"
)

// Unface is generated to dispatch src into the specific Un*er methods
// implemented on *URL. Unrecognised src types return ErrNotHandled so the
// library's plugin pipeline can try defaults.
func (recv *URL) Unface(src any) error {
	switch v := src.(type) {
	case string:
		return recv.Unstring(v)
	}
	if m, ok := unface.MapOf(src); ok {
		return recv.Unmap(m)
	}
	return unface.ErrNotHandled
}

// Unface is generated to dispatch src into the specific Un*er methods
// implemented on *Counter. Unrecognised src types return ErrNotHandled so the
// library's plugin pipeline can try defaults.
func (recv *Counter) Unface(src any) error {
	if n, ok := unface.NumberOf(src); ok {
		return recv.Unnumber(n)
	}
	return unface.ErrNotHandled
}

// Unface is generated to dispatch src into the specific Un*er methods
// implemented on *Event. Unrecognised src types return ErrNotHandled so the
// library's plugin pipeline can try defaults.
func (recv *Event) Unface(src any) error {
	switch v := src.(type) {
	case time.Time:
		return recv.Untime(v)
	}
	return unface.ErrNotHandled
}

// Unface is generated to dispatch src into the specific Un*er methods
// implemented on *Flags. Unrecognised src types return ErrNotHandled so the
// library's plugin pipeline can try defaults.
func (recv *Flags) Unface(src any) error {
	switch v := src.(type) {
	case bool:
		return recv.Unbool(v)
	case string:
		return recv.Unstring(v)
	}
	return unface.ErrNotHandled
}
